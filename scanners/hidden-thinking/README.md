# hidden-thinking

Writes evidentiary notes when it sees thinking blocks.

- `thinking.empty` (note) --- `yes` if text block is empty otherwise
  `no`

If a recent session contains empty thinking blocks, a finding task opens
an issue.

- `hidden-thinking` (issue) --- reference to `issue.md`
